ui.manual <- function() {
  tabItem(tabName = "manual",
          fluidRow(
            box(title = NULL, width = 6, 
                helpText("Manual has not yet been added")
            )
          )
  )
}